{
  "name": "powercompute-contracts",
  "version": "1.0.0",
  "description": "Test suite and Hardhat config for the PowerCompute ($PWR) protocol contracts. The dApp itself is deployed as static HTML with no build step — see README.md. This package.json exists only to run the tests in /test.",
  "private": true,
  "scripts": {
    "test": "hardhat test",
    "compile": "hardhat compile"
  },
  "devDependencies": {
    "hardhat": "^2.22.0",
    "@nomicfoundation/hardhat-toolbox": "^5.0.0"
  }
}
